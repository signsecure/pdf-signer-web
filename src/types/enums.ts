export enum SignaturePlacementType {
  PAGES = "pages",
  TEXT = "text",
}

export enum SignerNameText {
  NAME_ONLY = "name-only",
  SIGNED_BY_WITH_NAME = "signed-by-name",
  SIGNED_BY_SIGNER_NAME = "signed-by-signer-name",
  NONE = "none",
}

export enum TextPosition {
  ABOVE = "above",
  BELOW = "below",
}
